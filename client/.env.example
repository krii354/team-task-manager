# Local dev points the frontend at the standalone API server on :4000.
# For production / unified deploys, leave this UNSET — the client will use
# a relative `/api` URL on the same origin as the page.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_APP_NAME=Team Task Manager
